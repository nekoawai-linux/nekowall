# Build tree and release archives. `make` puts one in build/, `make dist`
# puts the other in dist/, and neither belongs in history.
/build/
/build-*/
/dist/

# What the tooling around a CMake project leaves behind
/CMakeUserPresets.json
/compile_commands.json
/.cache/
*.user

# Assistant tooling
/.claude/
/.codex/
/.agents/
/.cursor/
/.aider*
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/.github/copilot-instructions.md

# Editors and desktops
*~
*.sw?
.DS_Store
